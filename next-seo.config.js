const title = 'Pawi Store';
const description = 'Najlepszy sklep ever';

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    site_name: 'Pawi Store',
  },
};

export default nextSeoConfig;
